---
version: '3.7'
services:

  happy_predictions:
    restart: unless-stopped
    hostname: happy_predictions
    #    image: ${CI_REGISTRY}/happy_predictions:latest
    build:
      context: .
    ports:
      - 8443:8443
    env_file: .env
